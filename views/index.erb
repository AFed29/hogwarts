<h2>Student List:</h2>

<table>
  <tr>
    <th>Name</th>
    <th>House</th>
    <th>Age</th>
  </tr>
  <% @students.each do |student| %>
    <tr>
      <td><%= student.full_name %></td>
      <td><%= student.house.name %></td>
      <td><%= student.age %></td>
    </tr>
  <% end %>
</table>
